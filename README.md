Estimates transcript monitor - skeleton

Run the MVP CLI (uses fixtures):

python -m estimates_monitor.cli --run

Notes:
- Replace the openai_call_func in cli.main with a real OpenAI client implementation when ready.
- Install markitdown if testing real PDF parsing.
